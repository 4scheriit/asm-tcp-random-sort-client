# asm-tcp-random-sort-client
Description: Final project for Computer Architecture: a fully X86-64 Assembly TCP client that connects to a localhost server, requests random bytes, stores them to a file, sorts them, and saves both raw and sorted sections using procedure-based design with heap and stack usage.
